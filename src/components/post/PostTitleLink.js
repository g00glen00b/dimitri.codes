import styled from '@emotion/styled';
import React from 'react';
import {SimpleLink} from '../shared/SimpleLink';

export const PostTitleLink = styled(props => <SimpleLink {...props}/>)`
  font-weight: 400;
`;
